Keep the module dependency list defined when SPA plugins are disabled.
The upstream module build references it unconditionally for the echo-cancel
module even though it is initialized by the optional filter-graph plugin.

--- a/spa/meson.build
+++ b/spa/meson.build
@@ -5,6 +5,8 @@
 #pthread_lib = dependencies('threads')
 #mathlib = cc.find_library('m', required : false)
-
+# Keep this available to src/modules when spa-plugins is disabled.
+plugin_dependencies = []
+
 spa_dep = declare_dependency(
   include_directories : [
     include_directories('include'),
